#include "main.h"
#include <stdio.h>

/**
 * mul - multiply 2 integers
 * @a: first integer
 * @b: second integer
 * Return: Always 0
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
